**Trouve l'adresse !**

Les données sont en train d'apparaitre sur le darkNet, le pirate est en train de les uploader.

Trouve l'adresse qui consomme le plus pour le localiser !


En entrée : une liste de consommation.

Sur chaque ligne : la consommation (un nombre), un espace, puis l'adresse (au format numéro de rue ESPACE nom de rue).

Le résultat attendu : l'adresse (comme en entrée).

100 points pour la bonne réponse !